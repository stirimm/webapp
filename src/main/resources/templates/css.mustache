<style type="text/css">
    html, body {
        color: lightgray;
        background-color: #333;
        font-family: system-ui, -apple-system, sans-serif;
    }

    body {
        margin: 40px auto;
        max-width: 800px;
        line-height: 1.6;
        font-size: 18px;
        padding: 0 10px;
    }

    h1, h2, h3 {
        line-height: 1.2;
    }

    a {
        color: lightblue;
        text-decoration: none;
    }

    a:visited {
        color: mediumpurple;
    }

    .title {
        text-align: center;
    }

    .subtitle {
        text-align: center;
        color: #999;
        font-size: 16px;
        margin-top: -10px;
        margin-bottom: 12px;
        font-weight: normal;
    }

    .nav {
        text-align: center;
        padding-bottom: 20px;
    }

    .nav a, .nav strong {
        font-size: 14px;
        padding: 5px 16px;
        border-radius: 20px;
    }

    .nav a {
        color: #999;
    }

    .nav a:hover {
        background: #444;
        color: lightgray;
    }

    .nav strong {
        background: #4a4a4a;
        color: lightgray;
    }

    .nav .nav-sep {
        display: none;
    }

    .news-item {
        padding-bottom: 10px;
    }

    .news-description {
        text-align: justify;
    }

    .news-footer {
        display: flex;
        justify-content: space-between;
        align-items: baseline;
    }

    .news-meta {
        color: gray;
        font-size: 14px;
    }

    .source-label {
        color: #888;
        font-size: 13px;
    }

    .source-expand {
        font-size: 13px;
        text-align: right;
    }

    .source-expand summary {
        cursor: pointer;
        color: #888;
        list-style: none;
    }

    .source-expand summary::-webkit-details-marker {
        display: none;
    }

    .source-expand summary::before {
        content: '▸ ';
        color: #666;
    }

    .source-expand[open] summary::before {
        content: '▾ ';
    }

    .source-expand .source-list {
        margin-top: 6px;
        display: flex;
        flex-wrap: wrap;
        gap: 5px;
        justify-content: flex-end;
    }

    .source-expand .source-list a {
        color: #aaa;
        font-size: 12px;
        padding: 2px 8px;
        background: #3d3d3d;
        border-radius: 3px;
    }

    .source-expand .source-list a:hover {
        background: #4a4a4a;
        color: #ddd;
    }
</style>
